MODEL_ID=qwen3:4b
OLLAMA_HOST=http://localhost:11434
TAVILY_API_KEY=
PORT=8000
RATE_LIMIT_PER_MIN=30
CORS_ALLOW=http://localhost:3000,http://localhost:8000
GROQ_API_KEY=
GROQ_MODEL_ID=
CHAT_MAX_TOKENS=1024  # max tokens for Chat responses (not meta)
META_MAX_TOKENS=2048  # max tokens per variant during Meta-Evolution

# --- M1 Feature Flags (enable all) ---
FF_TRAJECTORY_LOG=1
FF_PROCESS_COST_REWARD=1
FF_OPERATOR_MASKS=1
FF_EVAL_GATE=1

# Reward weights (only used if FF_PROCESS_COST_REWARD=1)
REWARD_ALPHA=1.0
REWARD_BETA_PROCESS=0.2
REWARD_GAMMA_COST=-0.0005

# Meta defaults
META_DEFAULT_N=12
META_DEFAULT_EPS=0.3

# --- Phase 4 (AlphaEvolve‑lite) ---
# Auto-run Phase 4 loop after meta runs (0=off, 1=on)
FF_CODE_LOOP=0
# Loop mode: live applies patches; dry_run plans only
CODE_LOOP_MODE=live
# Phase 4 loop timeout (seconds) and hourly cap
CODE_LOOP_TIMEOUT_SECONDS=600
CODE_LOOP_MAX_PER_HOUR=3
# Decision thresholds
PHASE4_DELTA_REWARD_MIN=0.05
PHASE4_COST_RATIO_MAX=0.9
GOLDEN_PASS_RATE_TARGET=0.8
